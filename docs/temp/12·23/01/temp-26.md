需要执行“高强度体积恢复”，但不建议按你写的参数“一把梭”直接长训。你现在的观测已经说明：系统处于典型的 **拥挤相 / 体积塌缩相**（(R\approx0.49)、KNN Top-1 P50≈0.056、PMI rank≈1e5），在这个相里 PMI 不可能进入近邻排序；同时 Renorm Max=0.0189 是硬红线，说明当前动力学在塌缩态下已经数值不稳定，直接加大力会有较高概率把不稳定放大。

我建议的结论是：**需要做高强度体积恢复，但要分成“脱困（de-collapse）+ 受控 ramp-up + 闸门熔断”三段执行**。这样你既能把点云拉出拥挤相，又不会把 Renorm/Residual 拉炸。

---

## 1) 你当前“恢复失败”的诊断是合理的

* 你说“短程排斥不足、锚定无力”是对的：在 (d\sim0.05) 的拥挤相里，任何在小 (d) 区间梯度近似线性的排斥（例如导数 (\propto d)）都会弱到无法把点顶开；同时 (\lambda=1.0) 在 (dt=1e{-4}) 的尺度上确实可能太慢。
* 但更关键的是：**Renorm Max=0.0189** 说明此时已经不是“慢”，而是“刚性 + 数值漂移”共同出现。你需要先把恢复过程做成“受控”，否则把 A/λ 提高 10× 会把刚性再提高 10×。

---

## 2) 是否采用你给的 Aggressive 参数（A=5、λ=10、门控 d<0.5、scale 2.5x）

方向上合理，但我建议做两处改造后再执行：

### 改造 A：把“短程排斥”换成 **在小 d 处就有足够斜率** 的形式

你当前提到的 log-cosh 在极小 d 段可能仍偏软。体积脱困期更稳的是“强短程、可截断、可上限”的排斥，例如：

* (F_{\text{rep}} \propto \frac{1}{d+\epsilon}) 或 (\frac{1}{(d+\epsilon)^2}) 的变体，但要 **cap** 上限（避免爆炸）
* 或 Softplus/Huber 型：小 d 线性但斜率大、到阈值后饱和

如果你暂时不想改势函数，实现最小改动是：**把门控阈值 (\epsilon) 设得更贴近当前拥挤尺度（例如 0.1–0.2），并对 d<\epsilon 的部分单独放大**，不要只写 d<0.5 但在 d=0.05 时仍“几乎为 0”。

### 改造 B：把恢复期的数值稳定性作为硬闸门，而不是“希望它稳定”

在你把 A/λ 拉上去之前，至少做以下两项之一：

* 增加隐式求解精度（你之前 3→8 已非常有效）：恢复期建议进一步 **自适应迭代**（残差高就多迭代）
* 恢复期临时 **降低 dt**（例如 1e-4 → 5e-5），只用于 500–1000 steps 脱困段；脱困后再回到 1e-4

否则 Renorm Max 很可能再次上天。

---

## 3) 我建议的“高强度体积恢复”执行方案

### Stage 0：脱困初始化（推荐，成本最低，收益最大）

不要依赖动力学慢慢“爬出塌缩态”。直接对当前状态做一次 **径向重标定**（保持角向不变，只把半径拉到目标分布），把系统从 (R\approx0.5) 一步送到 (R\approx1.2–1.5) 的可分离相。
这不是“硬 freeze 投影”，而是一次性“de-collapse”操作，且你现在已经证明软正则能避免投影冲突。

验收：KNN Top-1 P50 立刻上升到 >0.12（或你们设的阈值），Renorm 不应飙升。

### Stage 1：Aggressive Recovery（500–1500 steps，受控 ramp）

用你提出的方向，但分档上升，不要一步到位：

* Repulsion A：1.0 → 2.5 → 5.0（每档 300–500 steps）
* Anchor λ：2.0 → 5.0 → 10.0（同节奏）
* 门控：先用 d<0.2（贴近拥挤相尺度）再扩大到 d<0.5
* scale 2.5x 可以保留，但要配合 Gate D（下面）闭环调节

硬闸门（任何触发立即停、回退到上一个档位）：

* Renorm Max > 1e-4 持续 100 steps
* Residual P99 上升超过 10×
* KNN Top-1 P50 反向下降（说明又在塌回去）

### Stage 2：回到 Phase II 正常训练（2k–10k steps）

当体积恢复后，再回到你们的 PMI/Def 退火。此时 PMI rank 才有机会从 1e5 级别降下来。

---

## 4) 你需要新增一个“体积/拥挤熔断器 Gate D”，否则会反复试错

建议固定两个指标做闭环：

* Gate D1：KNN Top-1 距离 P50（Word-only 候选集），目标 > 0.15
* Gate D2：半径分布的 R50/R90（不要只看均值）

这两个指标比“R>1.25”更直接，因为你真正想要的是“近邻尺度不要小到被密度淹没”。

---

## 5) 结论：要做，但按“受控高强度恢复”做

我建议你执行高强度体积恢复训练，但按上述三段法，并把 A/λ 的提升做成 ramp，同时把 Renorm/Residual/Top-1 P50 作为硬闸门。你现在已经具备两项关键保险（Gated Repulsion、Soft Anchor），只要再加上“脱困初始化 + 闸门闭环”，就能把系统从拥挤相拉回可分离相，让 PMI 的吸引开始在 KNN 排序里体现。

如果你能提供两项信息，我可以把 Stage 0 的“径向重标定”给出更精确的操作口径：

