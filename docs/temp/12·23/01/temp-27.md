
可以进入下一步，但我不建议“直接 5k–10k steps 一口气跑到底、且所有开关保持不变”。你现在确实完成了最难的“脱困”（P50 从 0.056 拉到 0.291，半径恢复到 1.88），这让 PMI 终于有了在几何空间里竞争的可能性；但你同时带着一个仍然偏高的数值风险信号：Renorm Max=7.24e-4，距离你们长期稳定线（<1e-4）仍差约 7×。它目前“可运行”，但在对齐阶段（PMI/Def 力开始真正重排角向）很容易再次被放大。

我对你推荐配置的评估与建议如下。

一、是否进入 Alignment Phase：结论

1. 进入是合理的。现在的点云稀疏度（d_nn≈0.29）已经从“密度灾难”中出来了，PMI 才可能在 rank 上产生可观下降。

2. 需要带条件进入：先跑一个短的“对齐预热段”（例如 500–1000 steps），确认 Renorm/Residual 不恶化，再进入 5k–10k 的长训。否则一旦对齐力开始改变角向结构，Renorm Max 很可能上冲，导致你又回到“可跑但指标失真”的状态。

二、对你给的参数逐项评估

1. Steps: 5000–10000
   同意。这是你们此前一直缺的“沉淀时间”，尤其在你们骨架较强的情况下，语义对齐不会在 1k 步内显著改变 rank。

但建议拆成：

* 预热 1k（快速判定稳定性与趋势）
* 通过后再追加到 5k、10k（每 2k 出一次 checkpoint + Gate 报告）

2. Volume: volume-control + radius-scale 1.0 + anchor-lambda 10.0
   总体同意。你们刚把体积恢复到目标附近（R≈1.88），对齐阶段最怕再次塌回拥挤相。λ=10 的 soft anchor 是合理的“体积保险”。

我唯一的建议是：对齐阶段不要再追求“更大半径”，优先保持 d_nn 分布稳定。建议把“拥挤 Gate”固化为硬闸门：KNN Top-1 P50 不得跌回 <0.15（跌破就暂停语义权重上调或加强短程排斥）。

3. Repulsion: repulsion-a 5.0（短程防撞）
   方向正确，但要明确两点，否则 repulsion 会再次变成“全局漂移源”：

* 必须严格门控：只在 d<ε 起作用（你之前的做法是 d<0.5，建议对齐期先把 ε 收紧到 0.2–0.3，避免它在中距离上干扰 PMI 的角向重排）。
* 排斥力要有上限（cap），否则在局部拥挤时仍会造成刚性尖峰，推高 Renorm/Residual。

4. De-collapse: OFF
   同意。de-collapse 是脱困用的“外科手术”，对齐阶段再炸一次只会把你辛苦恢复的结构扰乱掉。

三、你现在必须补上的“对齐期稳定性措施”

这是我对“是否可以进入下一步”的关键条件。

1. Renorm Max 仍偏高：对齐期建议提高隐式求解精度或降低初期 dt
   你已经验证过 fixed_point_iters 从 3→8 的巨大收益。现在 Renorm Max=7e-4，说明还有优化空间。对齐期建议二选一（或两者都做）：

* 固定迭代从 8 提到 10–12（对齐预热段用更高精度，之后可降回 8）
* 或对齐前 1000 steps 把 dt 降一档（例如 1e-4 → 5e-5），等 Renorm 稳定后再恢复

目标：把 Renorm Max 拉回到 ≤1e-4 或至少 ≤2e-4，再进行长训会更安全。

2. 语义力（PMI/Def）的权重调度必须重新启用“渐进 ramp”，不要一步到位
   你现在 PMI rank P50 仍 ~80k，说明“吹大气球”还没开始旋转微调。对齐期要避免一次性上大语义权重导致数值震荡。建议：

* PMI/Def 权重从小到大分段：例如 0.2 → 0.35 → 0.5（每段 500–1000 steps）
* 每段结束跑一次审计：Renorm Max、Residual P99、KNN Top-1 P50、PMI rank P50、以及 hit@K 曲线（K=50/200/2000）

四、对齐成功的判据（你们现在的“<1k”目标过于激进）

“PMI Rank P50 < 1k”作为最终目标可以保留，但我建议你们用阶段性目标，否则你会在 10k 步后仍觉得“失败”，但其实已经显著进步：

* Stage 1（1k steps）：PMI rank P50 从 ~80k 降到 <30k
* Stage 2（5k steps）：降到 <5k–10k
* Stage 3（10k steps）：再冲 <1k（这通常需要 Def edges + 更精细的损失/采样策略）

并且把 A(q)=hit@50 换成更有信息的指标：rank 分布 + hit@K 曲线。Top-50 在 140k 节点上太苛刻，容易掩盖趋势。

五、结论与推荐执行版本

可以进入 Alignment Phase，但我建议你把配置改成“受控启动版”：

* Steps：先 1000（预热）→ 再 5000 → 再 10000（视指标趋势）
* Volume：保持（radius-scale 1.0，anchor-lambda 10.0）
* Repulsion：A=5.0 可以保留，但必须严格门控（建议 ε=0.2–0.3）+ 力上限
* Solver：对齐预热段 fixed_point_iters 提升到 10–12 或 dt 降到 5e-5（二选一，最好两者结合）
* PMI/Def：分段 ramp，不要一步到位
* 硬闸门：KNN Top-1 P50 不得跌破 0.15；Renorm Max 不得持续上升；Residual P99 不得出现数量级恶化

按这个方式跑，你现在“体积已恢复”的优势才能转化为“PMI rank 实质下降”，而不会再次陷入数值或密度相的回退。
