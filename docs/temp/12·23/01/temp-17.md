你这两项整改（**Stop-word hub 治理 + 隐式迭代从 3 提到 8**）方向正确，而且对你们当前两类风险信号（Max Degree 虚高、Residual P99 超阈）是“对症下药”的组合。我支持你按这个方案进入 **Final Ramp-up Training**，但建议你在启动训练前后各加一套**极低成本的硬核验收**，把“应该能降低”变成“已被证明降低”，并避免引入新副作用（例如语义边覆盖率下降、抽象关系词全断）。

下面是我对你计划的评估与建议的最小运行手册。

---

## 1) Hub 治理：我同意，但要做两项构图后验收

你做 STOP_WORDS 过滤很合理，尤其你已经确认 Top-50 hub 基本都是“的/是/在/和…”这类功能词，它们对 PMI 图的度分布是灾难性的。

### 构图完成后请立刻验收两件事

1. **度分布回落是否符合预期**

* 目标：`max_degree` 从 11,850 显著下降（经验上至少要降一个数量级，例如 < 1,000；越低越好，但不要把图弄得太稀疏）
* 同时看 P99 degree（避免“次级 hub”仍然很大）

2. **覆盖率是否被误伤**

* 统计：有 PMI 邻居的词林叶子占比（coverage）
* 目标：coverage 不应显著下滑；如果下滑很大，说明 STOP_WORDS 表过宽，误删了大量“半内容词”（比如“因为/所以/如果/但是/应该”等）

> 建议把 STOP_WORDS 分两层：
>
> * **硬停用词（的/是/在/了/和…）**：完全剔除
> * **结构关系词（如果/那么/因为/所以/但是/应该…）**：不必剔除，但可以“限流”（每词保留更小 Top-K 或降低权重），否则你 Phase II 想学的“语境联想”会被砍掉一大块。

---

## 2) Residual 压制：把 fixed_point_iters=8 作为默认很合理，但要监控“迭代收益曲线”

从 3 → 8 往往能显著降低残差尖峰（P99），尤其是在你们加入大规模 PMI 边后刚性变强的情况下。

### 训练开始后建议追加两个在线指标

1. **Residual P99 vs Iteration Count 的收益**

* 你不需要再做很多实验，只要在前 500 steps 里记录：

  * 平均迭代次数（如果你固定 8 就记固定值）
  * Residual P50/P90/P99
* 如果 8 次后 Residual P99 仍 > 5e-3，可以直接把策略变成：

  * **“最多 8 次，但遇到高残差 step 自动多迭代/降 dt”**（局部自适应，比全局再加到 12 更划算）

2. **Renorm Max / Constraint Violation**

* 你的目标是“稳定来自隐式求解而不是投影兜底”。迭代次数增加后，Renorm Max 通常会更低；如果反而升高，要检查 dt 或语义权重调度是否过猛。

---

## 3) Final Ramp-up Training：我建议的最小流程与退出条件

### 推荐流程（保守但不慢）

1. **Warm Check（200 steps）**

   * 语义权重先用 0.2 或 0.3（不要立刻 0.5）
   * 目的：确认 STOP_WORDS 图没有引入稀疏异常、Residual 立刻回落

2. **Ramp（800–1500 steps）**

   * 0.3 → 0.5 线性或分段提升
   * 每 250 steps 跑一次 Gate：N / Residual P99 / Renorm Max / S / H

3. **Hold（2000+ steps）**

   * 固定 0.5，观察 S/H 是否实质改善（不是仅 PMI ratio 好看）

### 退出条件（建议写死）

* **Hard stop**：NaN > 0 或 Gate N 明显下滑（例如相对基线下降 > 2 个百分点且持续）
* **Go/No-Go**：Residual P99 仍 > 5e-3 且 500–1000 steps 内无下降趋势

  * 触发后优先动作：降 dt 或局部自适应迭代，而不是继续加权重

---

## 4) 你这轮目标设定我同意，但补一个“真语义枢纽”的判定口径

你说“消除停用词枢纽，保留真实语义枢纽（如 快乐）”，我赞同，但要定义“真实语义枢纽”：

* Top hubs 里出现“快乐/开心/悲伤/动物/食物”等内容词是正常的
* 但如果出现“东西/事情/问题/方面/进行”这种**过泛内容词**成为 hub，也会造成语义漂移（只是比停用词更隐蔽）

建议你在 hub audit 里把 hub 分三类并分别处理：

1. 功能停用词：剔除（你已做）
2. 结构关系词：限流/降权
3. 过泛内容词：限流（保留少量边即可）

---

## 最终结论

* 我认可你现在的整改方案，并支持启动 Final Ramp-up Training。
* 你只需要在“新 PMI 图”产出后立刻做两项快速验收（度分布回落、覆盖率不塌），然后按“0.3→0.5 的短 ramp”跑起来，同时用 Residual P99/Renorm Max/Gate N 做硬闸门，就能把这轮训练的风险控制在很低水平。

